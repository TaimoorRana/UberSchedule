<!DOCTYPE html>
<html>
<head>
  <title>UberScheduleApp - Edit Your Profile</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag('application') %>

</head>
<body>
<div>
  <p style="font-family:helvetica; font-size:20px; color:#000000; letter-spacing:2px; font-weight:bold;"><%= @student_first_name %> <%= @student_last_name %>'s Profile </p>
</div>
<div class="form" style="font-family:helvetica; font-size:13px; color:#000000;">
  <%= form_tag(:action => 'updateProfile') do %>

      Change your minor :
      <%= select_tag(:option, options_for_select(["General", "Computer Games",
                         "RealTime Embedded and Avionics Software", "Web Services and Applications"], selected: @current_opt))%></br>

    Select the periods you DO NOT want :</br>

    <table border="1">
      <tr style="font-family:helvetica; font-size:13px; color:#000000; font-weight:bold;">
        <th>Day</th>
        <th>AM</th>
        <th>PM</th>
      </tr>
      <tr>
        <td>Monday</td>
        <td><%= check_box_tag 'noMondayAm', '1', @previous_preferences.exists?(1) %></td>
        <td><%= check_box_tag 'noMondayPm', '1', @previous_preferences.exists?(2) %></td>
      </tr>
      <tr>
        <td>Tuesday</td>
        <td><%= check_box_tag(:noTuesdayAm, '1', @previous_preferences.exists?(3)) %></td>
        <td><%= check_box_tag(:noTuesdayPm, '1', @previous_preferences.exists?(4)) %></td>
      </tr>
      <tr>
        <td>Wednesday</td>
        <td><%= check_box_tag(:noWednesdayAm, '1', @previous_preferences.exists?(5)) %></td>
        <td><%= check_box_tag(:noWednesdayPm, '1', @previous_preferences.exists?(6)) %></td>
      </tr>
      <tr>
        <td>Thursday</td>
        <td><%= check_box_tag(:noThursdayAm, '1', @previous_preferences.exists?(7)) %></td>
        <td><%= check_box_tag(:noThursdayPm, '1', @previous_preferences.exists?(8)) %></td>
      </tr>
      <tr>
        <td>Friday</td>
        <td><%= check_box_tag(:noFridayAm, '1', @previous_preferences.exists?(9)) %></td>
        <td><%= check_box_tag(:noFridayPm, '1', @previous_preferences.exists?(10)) %></td>
      </tr>


    </table><br>
    Allow classes at the Loyola campus? :
      <%= select_tag(:noLoyolaCampus, options_for_select(['Yes', 'No'], selected: @noLoyolaCampus))%></br>
    Allow split classes? :
      <%= select_tag(:noSplitClasses, options_for_select(['Yes', 'No'], selected: @noSplitClasses))%>
      <br><br><br>
    <%= submit_tag("Edit Profile",  class: 'submitButton') %>
  <% end %> <!-- end of the form -->
</div>


<%= yield %>

</body>
</html>
